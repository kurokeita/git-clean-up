# Completion checklist

- Run `pnpm test` after changes.
- Run `pnpm run check` after changes.
- Update README/CLI usage docs when options or cleanup capabilities change.
- Preserve safety guarantees around destructive git actions and branch protection.
